imap:
  host: imap.example.com
  port: 993
  tls: true
  starttls: false
  insecure_skip_verify: false
smtp:
  host: smtp.example.com
  port: 587
  tls: false
  starttls: true
  insecure_skip_verify: false
auth:
  username: you@example.com
  # password is stored in the OS keychain or encrypted keyring file
keyring_backend: auto
defaults:
  drafts_mailbox: Drafts
